Uppercase tab strip with an oxblood underline.

```jsx
<Tabs items={['All', 'Color', 'Cuts', 'Treatments']} value={tab} onChange={setTab} />
```

For in-page filtering. Route-level navigation belongs in NavBar.
