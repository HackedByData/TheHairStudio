Single-line text field, on true black.

```jsx
<Input label="Full name" placeholder="First and last" required />
<Input label="Phone" icon="phone" type="tel" hint="We text a reminder the day before." />
```

Focus turns the border oxblood — no glow. Errors replace the hint and turn the border red.
