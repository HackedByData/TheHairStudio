Dashed placeholder for an empty list or filter.

```jsx
<EmptyState icon="calendar" title="No upcoming visits" message="Book a colour service and it will show up here." action={<Button size="sm">Book now</Button>} />
```

Keep the message to one sentence and always offer the action.
