Oxblood star rating with review count.

```jsx
<Rating value={4.3} count={514} />
<Rating value={5} label="Yelp" size={12} />
```

Round to one decimal. Never invent a rating — pull the real number or omit the component.
