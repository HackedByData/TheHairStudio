A menu line with dotted leader — the pricing-list signature.

```jsx
<ServiceRow name="Balayage" price="$295" from />
<ServiceRow name="Women's Haircut" price="$75" from note="Bra length or shorter" />
```

Every starting price must carry `from`. Prices are oxblood and tabular. Group rows under a `SectionHeading` per category and close the list with the length-and-density disclaimer.
