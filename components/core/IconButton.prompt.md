A square-or-round action carrying only a glyph.

```jsx
<IconButton icon="phone" label="Call the salon" variant="outline" />
<IconButton icon="x" label="Close" size="sm" />
```

`label` is required. Use `round` only for floating actions over photography.
