A small uppercase status marker.

```jsx
<Badge tone="confirmed" dot>Confirmed</Badge>
<Badge tone="accent">Master stylist</Badge>
```

Booking states map to `confirmed` / `pending` / `cancelled`. `solid` (bone fill) is for badges on photography.
