A pill-shaped filter chip — the only pill radius in the system.

```jsx
<Tag selected onClick={() => setFilter('color')}>Color</Tag>
<Tag onClick={() => setFilter('cuts')}>Cuts</Tag>
```

Use for gallery and service filters. A selected tag is an oxblood wash, never a solid fill.
